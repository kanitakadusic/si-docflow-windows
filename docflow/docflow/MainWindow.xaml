<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="docflow.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:docflow"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="docflow">

    <Grid>
        <StackPanel VerticalAlignment="Top" Orientation="Horizontal">
            <Button x:Name="PickAFileButton" Content="Open a file"
            Click="PickAFileButton_Click" IsEnabled="False" Margin="0,0,0,10"/>
            <TextBlock x:Name="PickAFileOutputTextBlock" TextWrapping="Wrap" Padding="20,5,0,0"/>
        </StackPanel>
        <DropDownButton x:Name="DropDownButton" Content="Pick a file type" >
            <DropDownButton.Flyout>
                <MenuFlyout Placement="Bottom">
                    <MenuFlyoutItem Click="MenuFlyoutItem_Click" Text=".pdf"/> <!--add here more types as time goes on-->
                    <MenuFlyoutItem Click="MenuFlyoutItem_Click" Text=".jpg"/>
                    <MenuFlyoutItem Text="soon..."/>
                </MenuFlyout>
            </DropDownButton.Flyout>
        </DropDownButton>
    </Grid>
</Window>
